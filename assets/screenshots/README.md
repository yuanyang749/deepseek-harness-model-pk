# 市场截图

把 1 到 8 张 PNG 放到这个目录，文件名与仓库根目录 `screenshots.json` 一致。路径不要改，市场按那个文件读图。

建议这 4 张，按这个顺序：

1. `01-create.png`：创建页。能看出一次选多个模型、填同一份任务。
2. `02-preflight.png`：预检页。开始前把任务和执行条件冻住。
3. `03-experiment.png`：对照进行中。各模型各跑各的，互不影响。
4. `04-report.png`：实验报告。自己排名次，可导出 PNG。

图片请用仓库里的真实界面，不要用外链图床。
